# V2-5.5 — Test Matrix

Commands after Gate B/C: `npm test`, `v2-5-5:scenarios`, `verify:v2-5-5-release-gate`, `build:win`.
Suites: perf-bench (median-3), scale-dataset, db-maintenance, reliability crash/soak, prior regression.
